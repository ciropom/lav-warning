per lanciare il programma:
paver run -d LAV_Warning/

per debug:
adb logcag | grep X

X puo` essere:
E: per vedere tutti gli errori
TiAPI: per vedere tutti i messaggi che stampi con 
Titanium.API.info() 

es:
adb logcat | egrep "TiAPI|(E/.*)"

